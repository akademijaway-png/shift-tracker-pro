name: ⏰ Keep the brain awake
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the brain 🧠
        run: curl -fsS https://shift-tracker-pro.onrender.com/api/health || true
